# MongoDB Atlas Connection String
# Replace YOUR_CLUSTER_URL with your actual cluster URL from MongoDB Atlas
MONGO_URI=mongodb+srv://renegaderaider:Cat%40Renagde.wtf73@YOUR_CLUSTER_URL/renegade_db?retryWrites=true&w=majority

# Note: The @ symbol is URL encoded as %40 in the password
# Once you get your cluster URL, replace YOUR_CLUSTER_URL and add this to Netlify environment variables